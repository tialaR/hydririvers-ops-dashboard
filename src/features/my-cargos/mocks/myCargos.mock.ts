/** @deprecated Import from `@/features/cargo/mocks/owned-cargos.mock` — shim de compatibilidade. */
export {
  carrier2CargosMock,
  carrierCargosMock,
  myCargosMock,
  shipper2CargosMock,
  userCargosMock,
} from '@/features/cargo/mocks/owned-cargos.mock';
