/**
 * Basemap público OpenFreeMap — somente rota dev `hydroway-map-spike` (V2.7c).
 * Não usar em produção `/cargas/[id]/mapa`; sem token Mapbox/Google/MapTiler.
 */
export const DEV_BASEMAP_STYLE_URL = 'https://tiles.openfreemap.org/styles/bright';
