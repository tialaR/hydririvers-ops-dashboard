/**
 * @deprecated Parallel DS package component — not used in mobile shell.
 * Mobile production uses `@/shared/components/icon-button` (`glass-compact-production`).
 * Kept for design-system catalog / future desktop migration only.
 */
export {
  DsIconButton,
  type DsIconButtonProps,
  type DsIconButtonSize,
  type DsIconButtonVariant,
} from './icon-button';
