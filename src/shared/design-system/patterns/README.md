# Design System Patterns

Reusable compositions of core components live here when they are broader than a single feature but are not low-level primitives.

Patterns may coordinate layout and interaction, but they must not own business rules such as cargo lifecycle, shipper permissions, operational risk, or negotiation state.
