# dependencies
node_modules/

# next
.next/
out/
build/
dist/

# tests / coverage
coverage/
test-results/
playwright-report/

# cache
.turbo/
.cache/

# env
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# macOS
.DS_Store

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# TypeScript
tsconfig.tsbuildinfo

# Mock persistence (dev only — never commit manual QA data or PII)
.mock-data/*.json

# editor / local scratch
*.tmp
*.bak
*.old

# capturas temporárias de revisão visual (não versionar)
.phase-visual-check/

# Vessel hero kit (material temporário — não versionar)
hydririvers-vessel-hero-kit/
hydririvers-vessel-hero-kit.zip
